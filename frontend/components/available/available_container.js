

import { connect } from 'react-redux';

import Available from './available';

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => {
    return () => dispatch();
};

export default connect(null, null)(Available);
