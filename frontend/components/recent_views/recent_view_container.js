

import { connect } from 'react-redux';

import RecentView from './recent_view';

const mapStateToProps = state => ({

});

const mapDispatchToProps = dispatch => {
    return () => dispatch();
};

export default connect(null, null)(RecentView);
